<?php
Admin::prep();
Builder::clear();

if(!Admin_User::isLoggedIn()){
  Builder::loadPart("admin_login");
} else if(!Admin::hasAccessTo(explode("/",Admin::getLoc())[0],0)){
  Builder::loadPart("admin_denied");
} else {
  include("cms/modules/" . Admin::getLoc());
}

Builder::end();
?>
